import "../globals.css";
 
export const metadata = {
    title: "Celestial Cast",
    description: "Join the glorious evolution",
  };

export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}